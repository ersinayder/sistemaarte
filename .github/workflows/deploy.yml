name: Deploy Producao

on:
  push:
    branches:
      - main

jobs:
  testes:
    name: Testes Backend
    runs-on: self-hosted
    env:
      HOMEPATH: C:\Users\Administrator
      HOME: C:\Users\Administrator
      NODE_ENV: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Matar processos Node travados e limpar node_modules
        shell: cmd
        run: |
          taskkill /F /IM node.exe /T 2>nul || echo sem processos node
          taskkill /F /IM esbuild.exe /T 2>nul || echo sem processos esbuild
          timeout /t 2 /nobreak >nul
          if exist "%GITHUB_WORKSPACE%\backend\node_modules" (
            rmdir /s /q "%GITHUB_WORKSPACE%\backend\node_modules"
          )

      - name: Instalar dependencias de teste
        shell: cmd
        working-directory: backend
        run: |
          set JAVA_HOME=C:\Program Files\Java\jdk-21
          set PATH=C:\Program Files\Java\jdk-21\bin;%PATH%
          npm install

      - name: Executar testes
        working-directory: backend
        run: npm test

  build-and-deploy:
    name: Build & Deploy
    runs-on: self-hosted
    needs: testes

    env:
      HOMEPATH: C:\Users\Administrator
      HOME: C:\Users\Administrator

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm install

      - name: Build frontend
        working-directory: frontend
        run: npm run build
        env:
          NODE_ENV: production

      - name: Deploy frontend dist
        shell: powershell
        run: |
          $source = "${{ github.workspace }}\frontend\dist\"
          $dest   = "C:\sistemaarte\frontend\dist"
          if (!(Test-Path $dest)) { New-Item -ItemType Directory -Path $dest -Force }
          robocopy $source $dest /MIR /NFL /NDL /NJH /NJS
          if ($LASTEXITCODE -le 7) { exit 0 } else { exit $LASTEXITCODE }

      - name: Stop PM2
        shell: powershell
        run: |
          $env:HOMEPATH = "C:\Users\Administrator"
          $env:HOME     = "C:\Users\Administrator"
          pm2 stop sistemaarte-backend 2>$null; $true
          Start-Sleep -Seconds 3

      - name: Sync backend source
        shell: powershell
        run: |
          $source = "${{ github.workspace }}\backend\"
          $dest   = "C:\sistemaarte\backend"
          if (!(Test-Path $dest)) { New-Item -ItemType Directory -Path $dest -Force }
          robocopy $source $dest /MIR /NFL /NDL /NJH /NJS `
            /XD node_modules data `
            /XF .env *.db
          if ($LASTEXITCODE -le 7) { exit 0 } else { exit $LASTEXITCODE }

      - name: Install backend dependencies
        shell: cmd
        run: |
          set JAVA_HOME=C:\Program Files\Java\jdk-21
          set PATH=C:\Program Files\Java\jdk-21\bin;%PATH%
          cd C:\sistemaarte\backend
          npm install --production --prefer-offline

      - name: Reload PM2 via ecosystem
        shell: powershell
        run: |
          $env:HOMEPATH = "C:\Users\Administrator"
          $env:HOME     = "C:\Users\Administrator"
          Set-Location "C:\sistemaarte\backend"
          $status = pm2 describe sistemaarte-backend 2>&1
          if ($status -match 'online|stopped|errored') {
            pm2 delete sistemaarte-backend 2>$null; $true
          }
          pm2 start ecosystem.config.js --env production
          pm2 save

      - name: Concluido
        shell: powershell
        run: Write-Host "Deploy concluido em $(Get-Date -Format 'dd/MM/yyyy HH:mm:ss')"
